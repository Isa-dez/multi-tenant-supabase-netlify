# multi-tenant-supabase-netlify
Uses Supabase as a multi‑tenant backend (org‑based) • Uses RLS so each user only sees their org’s data • Uses path‑based tenant routing like /acme/projects • Can be deployed to Netlify easily
